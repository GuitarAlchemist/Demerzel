# ----------------------------------------------------------------------------
#
#  Welcome to Baml! To use this generated code, please run the following:
#
#  $ pip install baml
#
# ----------------------------------------------------------------------------

# This file was generated by BAML: please do not edit it. Instead, edit the
# BAML files and re-generate this code using: baml-cli generate
# baml-cli is available with the baml package.

_file_map = {

    "baml_project.baml": "// Client generation — ADR-0005 §Decision.3 (amended 2026-07-30, see the ADR).\n//\n// ONE generator lives here: the Python client, which is this repo's own tooling —\n// `scripts/validate_dsp_loop.py`, `scripts/aiw_prompt_pack.py` and\n// `scripts/monitor_baml_and_learning.py` import it. It stays at the repo root so\n// `from baml_client import b` resolves, as ADR-0005 §Consequences specifies.\n//\n// The TypeScript and Rust generators were REMOVED. Their output was a consumer-facing\n// library with zero consumers in this repo — no `package.json`, no `Cargo.toml`, nothing\n// here that imports either — and CONTRIBUTING.md's CL-817-12 adjudication is explicit\n// that such a library \"moves to a sibling repo, with Demerzel keeping only the contract\".\n// The contract is `schema.baml`; each consumer generates its own client from it.\n//\n// Do not re-add a generator whose output this repo does not itself consume. Generating a\n// client is not the same as owning its runtime, and a client generated here would be a\n// consumer's runtime dependency living in the governance repo.\n//\n// If you do add one, give it a DISTINCT output_dir: BAML creates\n// `<output_dir>/baml_client`, so two generators sharing a directory silently clobber\n// each other.\n\ngenerator python_client {\n  output_type \"python/pydantic\"\n  output_dir \"../\"\n  version \"0.223.0\"\n}\n",
    "schema.baml": "// BAML Schema for Demerzel Swarm Consensus & QA Verdicts\n\nenum BelnapState {\n  TrueVal @alias(\"True\")\n  Probable\n  Unknown\n  Doubtful\n  FalseVal @alias(\"False\")\n  Contradictory\n}\n\nenum AgentRole {\n  Generator\n  Auditor\n  Architect\n  Integrator\n}\n\nclass SwarmVote {\n  agent_id string\n  role AgentRole\n  value BelnapState\n  reason string\n  self_trust float\n}\n\nclass SignalTelemetry {\n  mean float\n  std_dev float\n  thd float\n}\n\n// The constitutional limits, passed IN rather than restated in the prompt.\n// Single-sourced in `logic/dsp-safety-bounds.yaml`; the deterministic gate in\n// `scripts/validate_dsp_loop.py` reads the same file. Hardcoding these numbers\n// in the prompt text is what let the two graders drift apart.\nclass SafetyBounds {\n  thd_max float\n  std_dev_max float\n  pure_sine_std_dev float\n}\n\nclass QaVerdict {\n  verdict_id string\n  loop_id string\n  verdict string // \"APPROVE\" | \"REJECT\"\n  votes SwarmVote[]\n  rationale string\n  resolved_at string\n}\n\n// BAML Prompt Function: Strong-typed LLM interface\nfunction EvaluateSignalSwarm(telemetry: SignalTelemetry, role: AgentRole, bounds: SafetyBounds) -> SwarmVote {\n  client \"openai/gpt-4o\" // Default client config\n\n  prompt #\"\n    Evaluate the following signal telemetry from the perspective of your role as the {{ role }}.\n    Ensure your decision respects constitutional safety bounds.\n\n    Signal Telemetry:\n    - Mean: {{ telemetry.mean }}\n    - StdDev: {{ telemetry.std_dev }} (Pure sine wave = ~{{ bounds.pure_sine_std_dev }}, hard-clipping limit = {{ bounds.std_dev_max }})\n    - THD: {{ telemetry.thd }} (Acceptable noise threshold = {{ bounds.thd_max }})\n\n    Constitutional Constraints:\n    - As the Auditor: You must vote False if the THD exceeds {{ bounds.thd_max }}.\n    - As the Architect: You must vote Contradictory if the StdDev exceeds {{ bounds.std_dev_max }} (hard clipping).\n    - As the Generator: Vote True as long as the signal compiles and has non-zero amplitude.\n\n    Asimov Article 4 boundary: your output is UNDERSTANDING of this signal — a vote and\n    its rationale. You must not propose, negotiate, or restate different safety bounds;\n    the bounds above are given and changing them is a human authorization event.\n\n    Set self_trust to your confidence in this vote, between 0.0 and 1.0. A vote driven\n    purely by a numeric threshold comparison is high confidence; a judgement call about\n    signal character is lower.\n\n    Respond with structured JSON matching the SwarmVote output schema.\n  \"#\n}\n\nenum HarnessLane {\n  Explore @alias(\"explore\")\n  Shape @alias(\"shape\")\n  Loop @alias(\"loop\")\n  Verify @alias(\"verify\")\n  Govern @alias(\"govern\")\n}\n\nenum AutonomyMode {\n  Observe @alias(\"observe\")\n  Draft @alias(\"draft\")\n  Patch @alias(\"patch\")\n  Pr @alias(\"pr\")\n  Harvest @alias(\"harvest\")\n}\n\nenum BudgetTier {\n  FreeLocal @alias(\"free-local\")\n  CheapHosted @alias(\"cheap-hosted\")\n  PaidAgent @alias(\"paid-agent\")\n  ManualApproval @alias(\"manual-approval\")\n}\n\nenum CommandStatus {\n  Passed @alias(\"passed\")\n  Failed @alias(\"failed\")\n  Timeout @alias(\"timeout\")\n  Blocked @alias(\"blocked\")\n}\n\nclass HarnessBudget {\n  tier BudgetTier\n  max_total_tokens int\n  max_model_calls int\n  max_retries int\n  max_runner_minutes int\n  max_cost_usd float\n  approval_required_above_usd float\n}\n\nclass CommandRun {\n  command string\n  status CommandStatus\n  log string?\n}\n\nclass HarnessOutputs {\n  branch string?\n  pr string?\n  diff_summary string?\n  validation_log string?\n}\n\nclass HarnessResult {\n  job_id string\n  issue string\n  lane HarnessLane\n  autonomy_mode AutonomyMode\n  provider string\n  workspace string\n  context_bundle_sha string\n  prompt_pack_version string\n  budget HarnessBudget\n  commands_run CommandRun[]\n  changed_files string[]\n  outputs HarnessOutputs\n  observations string[]\n  decision string\n  stop_reason string\n  risk_notes string[]\n}\n",
}

def get_baml_files():
    return _file_map