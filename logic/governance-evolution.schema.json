{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/GuitarAlchemist/Demerzel/schemas/logic/governance-evolution",
  "title": "Governance Evolution Log",
  "description": "Tracks governance artifact effectiveness over time — citation counts, violation rates, promotion/deprecation candidates, and events for meta-compounding",
  "type": "object",
  "required": ["id", "artifact", "artifact_type", "metrics", "events", "assessment", "created_at", "last_updated"],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^gel-[a-z0-9-]+$",
      "description": "Unique governance evolution log entry identifier"
    },
    "artifact": {
      "type": "string",
      "description": "File path of the governance artifact being tracked (e.g., policies/scientific-objectivity-policy.yaml, constitutions/default.constitution.md)"
    },
    "citation_paths": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Tracked paths whose mentions count as citations of this artifact, for entries whose artifact is not itself a tracked path. An empty list marks an artifact that lives outside this repository and cannot be measured here. Read by scripts/evolution_metrics.py."
    },
    "artifact_type": {
      "type": "string",
      "enum": ["constitution", "policy", "persona", "schema", "contract", "meta", "capability", "skill", "runtime_code"],
      "description": "Type of governance artifact. 'meta' covers cycles/skills that operate over the framework itself; 'capability' covers cross-cutting features (e.g. visual-governance, harness-engineering) that are not a single file; 'skill' is an authored Claude skill; 'runtime_code' is consumer-owned code (e.g. ix crates) tracked here because Demerzel governs its lifecycle."
    },
    "metrics": {
      "type": "object",
      "required": ["citation_count", "violation_count", "compliance_rate", "last_cited", "last_violated", "promotion_candidate", "deprecation_candidate"],
      "properties": {
        "citation_count": {
          "type": "integer",
          "minimum": 0,
          "description": "Number of tracked files that mention the artifact (or its citation_paths), excluding its own files, archived/, state/evolution, state/quality-trend and the generated manifest. Computed by scripts/evolution_metrics.py when citation_source is git-grep-v1; hand-entered before 2026-09-13."
        },
        "violation_count": {
          "type": "integer",
          "minimum": 0,
          "description": "Number of times agents have violated this artifact"
        },
        "compliance_rate": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Proportion of compliant assessments (1 - violations/total_assessments)"
        },
        "last_cited": {
          "type": ["string", "null"],
          "format": "date-time",
          "description": "Commit time (UTC) of the newest commit that added or removed a mention; null when none ever did. Computed with citation_count."
        },
        "last_violated": {
          "type": ["string", "null"],
          "format": "date-time",
          "description": "Timestamp of most recent violation"
        },
        "promotion_candidate": {
          "type": "boolean",
          "description": "True if usage exceeds promotion threshold (e.g., 3+ citations, sustained high compliance)"
        },
        "deprecation_candidate": {
          "type": "boolean",
          "description": "True if artifact is unused for extended period (e.g., 90+ days uncited)"
        },
        "citation_source": {
          "type": "string",
          "enum": ["git-grep-v1"],
          "description": "How citation_count and last_cited were produced. Absent means hand-entered. scripts/quality_trend.py marks a change of source as a rebaseline."
        }
      }
    },
    "events": {
      "type": "array",
      "description": "Chronological record of governance events involving this artifact",
      "items": {
        "type": "object",
        "required": ["type", "context", "timestamp"],
        "properties": {
          "type": {
            "type": "string",
            "enum": [
              "cited",
              "violated",
              "amended",
              "promoted",
              "demoted",
              "superseded",
              "created",
              "compounding_cycle",
              "pattern_detected",
              "governance_concept_created",
              "cross_repo_validation",
              "kaizen_proposal",
              "gap_identified"
            ],
            "description": "Type of governance event. Schema-follows-reality: enums are extended to match actual usage. New event types may be added as governance evolves; pair extensions with a compound-cycle review to avoid accidental vocabulary."
          },
          "context": {
            "type": "string",
            "description": "What happened — decision ID, directive ID, violation details, etc."
          },
          "timestamp": {
            "type": "string",
            "format": "date-time",
            "description": "When this event occurred"
          }
        }
      }
    },
    "assessment": {
      "type": "object",
      "required": ["effectiveness", "recommendation"],
      "properties": {
        "effectiveness": {
          "$ref": "https://github.com/GuitarAlchemist/Demerzel/schemas/tetravalent-state",
          "description": "Tetravalent belief state (T/F/U/C) assessing whether this artifact is effectively serving its purpose"
        },
        "recommendation": {
          "type": "string",
          "enum": ["maintain", "promote", "demote", "deprecate", "investigate"],
          "description": "Recommended action based on metrics and events"
        }
      }
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "When this log entry was created"
    },
    "last_updated": {
      "type": "string",
      "format": "date-time",
      "description": "When this log entry was last updated"
    }
  }
}
