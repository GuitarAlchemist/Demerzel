{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/GuitarAlchemist/Demerzel/schemas/loop-state",
  "title": "Loop State",
  "description": "Tracks the orchestration state of a governed Ralph Loop (autonomous iterative development cycle). This schema is relaxed to the minimal core shape currently emitted by scripts/run_afk_cycle.py; a follow-up ticket will enrich the emitter to restore full auditability fields.",
  "type": "object",
  "required": ["loop_id", "goal", "repo", "status", "iterations"],
  "properties": {
    "loop_id": {
      "type": "string",
      "pattern": "^loop-[0-9]{4}-[0-9]{2}-[0-9]{2}-[0-9]{3}$",
      "description": "Unique loop identifier (e.g., loop-2026-06-23-001)"
    },
    "goal": {
      "type": "string",
      "description": "What the loop is trying to accomplish"
    },
    "repo": {
      "type": "string",
      "description": "Which repo the loop operates in (e.g., demerzel, ix, tars, ga)"
    },
    "risk": {
      "type": "string",
      "description": "Risk classification determined before the loop starts (e.g., low, medium, high, critical)"
    },
    "governance_mode": {
      "type": "string",
      "description": "Governance oversight mode (e.g., boundary-only, per-iteration)"
    },
    "status": {
      "type": "string",
      "description": "Current loop status (e.g., in_progress, completed, halted, escalated)"
    },
    "iterations": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["iteration", "timestamp", "action"],
        "properties": {
          "iteration": {
            "type": "integer",
            "minimum": 1
          },
          "timestamp": {
            "type": "string",
            "format": "date-time"
          },
          "action": {
            "type": "string",
            "description": "What the worker did in this iteration"
          },
          "outcome": {
            "type": "string",
            "description": "Result of the iteration (e.g., progress, shipped, blocked, halted)"
          },
          "governance_decision": {
            "type": ["string", "null"],
            "description": "Governance gate decision for this iteration, if any"
          },
          "belief_state": {
            "$ref": "https://github.com/GuitarAlchemist/Demerzel/schemas/tetravalent-state",
            "description": "Tetravalent belief state for the iteration outcome (optional)"
          }
        }
      }
    },
    "stall_count": {
      "type": "integer",
      "minimum": 0,
      "description": "Count of consecutive iterations without measurable progress"
    },
    "max_iterations": {
      "type": "integer",
      "minimum": 1,
      "maximum": 25,
      "default": 10,
      "description": "Hard cap on iterations (default 10, absolute max 25)"
    },
    "authorization_artifact": {
      "type": "string",
      "description": "Reference to the artifact that authorized the loop (e.g., github_issue:#381)"
    },
    "started_at": {
      "type": "string",
      "format": "date-time"
    },
    "completed_at": {
      "type": "string",
      "format": "date-time"
    },
    "created_at": {
      "type": "string",
      "format": "date-time"
    },
    "last_updated": {
      "type": "string",
      "format": "date-time"
    }
  }
}
